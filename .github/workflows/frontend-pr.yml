name: "PR Frontend"

on:
  pull_request:
    paths:
      - 'src/frontend/**'
      - '.github/workflows/**'
    branches:
      - main

jobs:
  docker-workflow:
    uses: ./.github/workflows/docker-pr.yml
    with:
      project: frontend