name: "PR Frontend"

on:
  pull_request:
    paths:
      - 'src/frontend/**'
      - '.github/workflows/**'
    branches:
      - main

jobs:
  docker-workflow:
    uses: ./.github/workflows/_docker-pr.yml
    with:
      project: frontend