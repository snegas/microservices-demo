name: "PR Email Service"

on:
  pull_request:
    paths:
      - 'src/emailservice/**'
      - '.github/workflows/**'
    branches:
      - main

jobs:
  docker-workflow:
    uses: ./.github/workflows/docker-pr.yml
    with:
      project: emailservice