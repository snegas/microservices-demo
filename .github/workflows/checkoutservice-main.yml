name: "Main Checkout Service"

on:
  push:
    paths:
      - 'src/checkout/**'
      - '.github/workflows/**'
    branches:
      - main
jobs:
  docker-workflow:
    uses: ./.github/workflows/_docker-main.yml
    with:
      project: cartservice